# Lightweight dev image — just install deps and run next dev
# Used only with docker-compose.dev.yml (source mounted as volume)
FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci
EXPOSE 3000
CMD ["npm", "run", "dev"]
